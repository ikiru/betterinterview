<%= simple_form_for(@interview_question) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :question %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
